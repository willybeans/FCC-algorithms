
function uniteUnique(arr) {
//  var newArr = arr.map();

 /* arr.reduce(function(currentValue){
    newArr.push(currentValue);
  }); */

  //return arr;
  console.log(arr);
}

uniteUnique([1, 3, 2], [5, 2, 1, 4], [2, 1]);
